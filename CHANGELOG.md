# 更新日志

先解释一下版本号,免得看着像降级:v1.0.0 → v2.0.11 是开发期的内部迭代号,问题查完、方案定稿后,正式版改用语义化版本 **1.0.0-20260905**(1.0.0 = 定稿,20260905 = 定稿日期)。正式版包含 v2.0.11 的全部内容,并在此基础上做了发版前终审清理。

按用户视角记录:每版写"你能感知到什么变化",排查细节看仓库里的报告文件。

## 1.0.0-20260905(正式版)— 2026-09-05

定稿形态。过曝根因在 SurfaceFlinger 合成端,由 KSU 模块的一行 prop 修掉;比率链由 LSPosed 模块接管。两个组件配合,最终状态:

- UltraHDR 照片恢复正常提亮,高光不死白、渐变完整,任何亮度档位都不过曝(实机验证)。
- HDR 触发回到 Android 15 的观感:跟着亮度动画渐进,不再"卡一下然后突然变化"。
- 比率上限开关真的能用了(此前从 v1.x 起就是死代码),实测 max=1.3 时全域精确钳在 1.3。
- App 做了发版前终审清理:删掉双重失效的"检查更新"和 GitHub 入口(应用没有网络权限、仓库又是私有的,这个功能从出生起就不可能工作);关闭总闸改为二次确认,第一次点击有提示,5 秒不确认自动复位;亮度扫描中途退出也会恢复原亮度;面板峰值统一显示实测的 750nits(此前界面写 800,和模块的钳制值矛盾)。
- 版本号 versionCode 33,与模块 prop、App 三方一致。

## v2.0.11 — 2026-09-05

- 修复"HDR 提亮时卡一下然后突然变化":此前写的是曲线终点值,一步跳到位;现在改回 A15 的算法,用实测的双通道亮度逐帧跟随面板抬亮动画,动画到哪比率跟到哪,收敛后数值与原来完全相同——过程平滑了,结果没变。
- 完成发版前两路独立终审的全部整改(其余条目见上方正式版)。

## v2.0.10 — 2026-09-05

- 过曝终局修复落地:KSU 模块 v1.1.0 发布,开机设置 `debug.sf.fp16_client_target=true`,实机验证过曝消除。顺带修掉日志文件的 EACCES 问题(开机用 root 预建并改属主)。
- App 修了一个从 v1.x 延续至今的死代码:比率上限读取反射了一个 AOSP 里根本不存在的方法,异常被静默吞掉,上限从来就没生效过。
- 面板峰值按实测改回 750nits,钳制数学随之校准。

## v2.0.9 — 2026-09-05

- App 首次打开时自动建好日志目录,并把日志镜像到 `/storage/emulated/0/hdr日志/`,普通文件管理器终于能直接看到日志了(此前查明:模块自身被 SELinux 永久禁止写 sdcard,只能由 App 用 root 权限镜像)。
- 状态页新增 root 授权状态,未授权时给出打开 SukiSU 管理器授权的引导(这个管理器不会弹被动授权窗)。

## v2.0.8 — 2026-09-05

- 审查修复轮:导出日志可能为空的回归修掉;关闭总闸的语义补完整(把字段和事件都归位);防重复触发的护栏;root 命令统一加超时和错误输出排空,管理器卡死时不再把 App 挂住。

## v2.0.7 — 2026-09-05

- 模块改为"持续写入者":每次都按捕获到的实际 SDR 亮度重算并写入比率,数值终于和面板实际抬亮同步(此前的"让位"逻辑建立在一个错误判断上,导致字段冻结在旧值)。
- App 日志页新增"从 logcat 抓取模块诊断",文件日志通道出问题时这里是兜底;再加 root 自愈:日志文件被删后自动重建。

## v2.0.6 — 2026-09-05

- 挖出一个阴坑:开关页的"恢复全部默认"会把日志开关永久写成关(它是 persist 属性,重启也不恢复),这就是"日志文件从来不出现"的完整原因。已修复:恢复默认一律把日志写回开。
- 开关页新增模式下拉(同源/禁抬亮),免重启切换,模块核心功能从此有了界面入口。
- 状态页显示当前模式,模块版本和 App 版本不一致时提示需要重启。

## v2.0.5 — 2026-09-05

- 日志开关处于关闭状态时,不再吞掉关键诊断:安装结果、模式切换、引导决策等 33 处关键日志绕过开关强制落盘。
- 亮度读取改为直接调用系统自己的换算函数,不再依赖内部字段的更新时序,读不到值的问题源头解决。

## v2.0.4 — 2026-09-05

- 首轮扫描日志定案:比率恒为 1.5 是因为亮度读取失败、走了兜底值。修法是新增亮度捕获点,把系统刚写入的实际双通道亮度缓存下来作为第一优先值源。
- 日志文件加 5 分钟心跳行,日志"看起来没在写"时可以立刻自证写入器是否活着。

## v2.0.3 — 2026-09-05

- HDR 测试页新增"一键亮度扫描":自动从 30% 缓变到 60% 再到 100%,每档同时采 App 通道和 root 通道(dumpsys)的数值,全程记录到 `/sdcard/Download/hdrscan.log`,可以直接分享给别人看。

## v2.0.2 — 2026-09-05

- 修"清空日志之后日志就再也不写了":删除文件会让系统侧还在握着的写入句柄变成孤儿,后续写入全部静默丢失;清空改为原地截断,文件、属主、句柄都保住。
- 日志页显示实际生效的路径,不再显示一个从未建成的"理想路径"。

## v2.0.1 — 2026-09-05

- 修 App 读不到比率值的真正原因:显示信息有三层缓存,全靠事件驱动刷新,只写值不发事件等于白写。现在引导写入后走系统原生的事件链,App 端终于能看到值。
- 日志文件被删后 2 秒内自动重开,不再静默断写。

## v2.0.0 — 2026-09-05

- 根因模型翻案:三个固件逐字节对比 + AOSP 源码全链核实,证明此前对"系统比率链还活着"的判断是错的——新系统上调用点根本没人路由。修复思路从"抢写入权"改为"同源引导":值源与面板实际抬亮同源,系统是唯一权威写入者。
- 新增三模式,免重启切换:0=同源引导(默认)、1=禁面板抬亮(过曝物理绝迹的保守选项)、2=旧版对照(排障用)。

## v1.1.0 – v1.1.8 — 2026-09-01 ~ 09-02(弯路时期,如实记录)

诊断密集期,APK 只在测试目录流通,没有公开发布。结果先说:1.1.7 起引入了"任何亮度都过曝"的严重回归,是全项目表现最差的一段,直到 v2.0.0 推翻重做才解决。

- 1.1.0–1.1.6:纯复刻 A15 链路的多轮尝试,逐层追踪断点,修好了"App 读不到值"的缓存链(这部分成果被 v2.0.x 继承)。
- 1.1.7–1.1.8:为解决写入竞争短路了系统链路并强制改判定,后来证明那两刀其实是无操作;真正让全亮度段过曝的是写入口的失真值——把抬亮后的主通道亮度错当 SDR 亮度去查曲线,低亮度失配 2.5~4 倍。方向错了,越加固越糟。

给后来者的一句话:修复显示链时,"接上原生链路"优于"抢过来自己写"。

## v1.0.0 – v1.0.10 — 2026-09-01(首日开源与连续修复)

- **1.0.0** 首版开源:LSPosed 模块(A15 比率语义 + 物理钳制 + 开机 GPU 合成绕 DPU 闪屏 + 落盘日志)+ Compose/Miuix 伴侣 App(状态 / HDR 测试 / 开关 / 日志 / 关于)。
- **1.0.1** 修启动崩溃(漏调 super.onCreate)。
- **1.0.2** HDR 样图挪进独立全屏窗口,色彩模式不再泄漏到 App 其他界面;新增主题切换和动态取色。
- **1.0.3** 修过曝回归:回退通道的默认上限改回实测验证过的 2.0(上一版"不限"让低亮度比率高到 3.7 倍以上)。
- **1.0.4** 修满亮度必过曝:去掉人为的 1.2 倍抬升,满亮度时诚实地按面板真实余量钳制。
- **1.0.5** 第一次接近根治:改用联想自己的字段按 A15 正统公式复算(真除式 + 物理钳制),一个钩子同时修过曝、App 读不到值、HDR 不触发三个问题;日志改为强制落盘。
- **1.0.6** 日志改双路径独立写入,一路失败不拖死另一路。
- **1.0.7** 架构重构:实测确认本机亮度通道无 HDR/SDR 分离,真除式公式在本机恒等于 1,改用厂商校准表 + 唯一权威写口。
- **1.0.8** 修测试任何 App 时日志里看不到记录的问题。
- **1.0.9** 用实机 dumpsys 提取的 30 点厂商 spline 替代此前估算的映射表;物理上限校准为 800nits(后来在 2.0.10 校准为实测 750)。
- **1.0.10** 修"写一次就冻结"的 bug,比率恢复随亮度持续更新。
