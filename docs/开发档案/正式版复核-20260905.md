# 正式版复核报告（2026-09-05）

复核对象：工作区未提交改动（`git diff HEAD`），涉及 3 文件：
- `app/src/main/java/com/tb710fu/hdrfix/HdrRatioFix.java`（+105/-33 核心）
- `app/build.gradle.kts`（versionCode 33 / versionName 1.0.0-20260905）
- `app/src/main/java/com/tb710fu/hdrfix/ui/Shell.kt`（APP_VERSION 同步）

复核方式：全量通读源码 + `javac` 单文件编译验证（JDK21 + xposed-stubs.jar + android-37，EXIT=0）+ Python 逻辑仿真（`工具-复核-正式版.py`，9/9 PASS）+ 全局 rg 交叉搜索。

---

## 一、逐项结论

### 1. FileLog 轮转修复 —— PASS

| 验证点 | 结论 | 依据 |
|---|---|---|
| 轮转后 writer 指向新文件 | ✓ | L1000-1011：oversize → rename → `f = new File(PATHS[i])` → close 旧 writer 置 null → 落入下方 `sWriters[i] == null` 分支用新 f 重开。仿真确认 writer 绑定路径始终正确，串路径计数=0 |
| 并发安全 | ✓ | `write()` 为 `static synchronized`（L963），rename/close/reopen 全在类锁内串行；自愈段同锁。仿真 4 线程并发无损坏 |
| .old 只保留一份 | ✓ | L1002：rotate 前 `if (old.exists()) old.delete()`。仿真无 .old2 堆积 |
| 存储上限恒定 | ✓ | 单路径 main ≤ 128KiB+一行、.old ≤ 128KiB+一行；实测仿真峰值 145,656B（上限 262,584B）。/data/system 仅承载第一路径（sdcard 为镜像），占用恒定有界，永不塞满 |
| rename 在 close 之前（打开中 rename） | ✓ 可接受 | Android/Linux 允许 rename 打开中的文件；close 前无写入（同锁内紧接 close），.old 定稿于超限瞬间 |

建议级备注：`f.renameTo(old)` 返回值未检查（L1003）。失败时退化为"不轮转但零数据丢失"，每条消息重试；同目录 rename 在 system_server 对 /data/system 几乎不可能失败，风险极低。

### 2. 日志开关语义收窄 —— PASS（带 1 项重要问题）

四类声明行（RATIO L383 / bootstrapDiag×6 / nitsCapture L445 / DISABLED L348）确认已从 logAlways 降为 log() ✓。

**关闭开关后仍会出现的行（全量枚举，29 处 logAlways）**：

| 类别 | 行 | 评价 |
|---|---|---|
| 加载 | loaded v(L140)、disabled at load(L146) | 合理 |
| 安装 | INSTALL OK/FAIL ×8(L206-469)、hooks installed(L472) | 合理 |
| 错误级 | FIELD FAIL(L333)、hookObs err(L404)、nitsCompute FAIL(L450)、fullBootstrap ×3(L694/702/715)、legacyWrite FAIL(L763)、SysProps FAIL(L909/912)、gpu err(L481)、init failed(L488) | 合理 |
| mode | MODE -> m(L519，一次性边沿) | 合理 |
| 一次性动作 | force GPU(L845)、legacy Set(L766，仅 mode2) | 可接受 |
| **诊断观察** | **apply CALLED(L271，2s 节流)**、SYSTEM handle(L199，1s 节流) | **问题①，见下** |

- **问题①（重要）**：L271 `HdrBrightnessModifier.apply CALLED` 仍为 logAlways，且该行在 disable 检查**之前**（L262-274）——用户关闭日志开关、甚至拉起 disable 总开关后，只要 HDR 抬亮链活动就仍每 2s 写一条 FileLog（v2.0.7 实证抬亮链是活的，此行必然出现；~70B×1800 条/小时 ≈ 126KB/h，约 1 小时触发一次轮转）。L199 `SYSTEM handle nits=` 同类，但运行时 handle 不被调（hasSpline=false 定案）实际不出现。**与"关闭开关后只剩加载/安装/错误级"的目标不符，建议两行同改 log()**（各一行改动）。
- **问题②（重要→建议）**：diag 节流 5s→60s **只落地 liveDiv 主路径 1 处**（L574）；其余 5 处（adapter/fieldSdr/fieldMain FAIL L595/608/621、NO nits source L631、src= 最终行 L651）仍为 5000L。高频路径（动画期每帧走 liveDiv 提前 return）已覆盖，低频/错误路径保留 5s 可辩护，但与"60s 落地"的声明不完全一致，非功能 bug。

### 3. prop 缓存（500ms）—— PASS

| 验证点 | 结论 |
|---|---|
| 延迟可接受性 | ✓ 用户 setprop 后最多 500ms 生效；仿真实测感知滞后 ~50ms（刷新点随机）；F 写口回调频率极高（每 binder 查询），disable/mode/userMax 三者均无安全边界问题 |
| volatile 正确性 | ✓ 4 字段（sPropCacheMs/sCachedDisabled/sCachedMode/sCachedUserMax）全 volatile；check-then-act 无锁竞态两线程同时刷新→幂等同值，无害；先写时间戳后填值的窗口内另一线程读到上一轮值，延迟量级不变。仿真 2.5s 窗口值域完整无卡死 |
| 读点覆盖 | ✓ HOOK1 before(after 走 currentMode)、HOOK2、HOOK3(L275 双查)、F 写口(L343+L355)、bootstrapRatio 两处 userMax(L569/L643)、legacyWrite(L746) 全部走缓存 |
| 直读点排查 | ✓ 无行为分叉。加载期 L141/145/475 直读为一次性合理；L655 日志行直读 PROP_MAX（值与 sCachedUserMax 相同，仅多一次反射，建议改用缓存值）；L866 log() 读 PROP_LOG 未入缓存（调用点均已节流至秒级，可接受） |

### 4. nitsCompute Method 缓存 —— PASS

- 并发首次竞争：两线程同时 null → 各自 getMethod（幂等、返回等价 Method）→ volatile 覆盖写，终态正确；半初始化态（B2B 已写、B2N 仍 null）→ 第三线程重取两个，无害 ✓
- setAccessible：目标是 `$1` 匿名类方法（注释标 public final），严格说非必需，保留对 hidden-API/跨 classloader 稳妥，一次性开销 ✓
- invoke 装箱：`float b` → 自动装箱 Float 匹配 `Object...`，返回 `(Float)` 强转 ✓
- **受检异常**：`Method.invoke` 声明 throws IllegalAccessException/InvocationTargetException，均在既有 `catch (Throwable)`（L449）内——javac 编译通过（EXIT=0）实证 ✓
- 失败自愈：getMethod 半途抛异常 → 下一调用重新获取；invoke 运行时抛 → 缓存保留（方法本身有效），合理 ✓

### 5. 性能评估（热路径通读）—— PASS

| 路径 | 改动前 | 改动后 |
|---|---|---|
| F 写口（每 binder 查询，动画期每帧） | 2 次反射 prop 读（disable+mode） | ≤2 次/500ms，平时 0 次 |
| setDisplayBrightness（动画期高频） | 4×callMethod（每次内部 findMethod 查找+setAccessible） | 首次 2×getMethod，此后 4×invoke 纯调用 |
| bootstrapDiag 日志（动画主路径） | 每 5s 一条 logAlways（拼接+双文件写+flush） | 每 60s 一条且受开关 |

剩余开销均为建议级：log() 的 PROP_LOG 每次反射 invoke（1 次/秒封顶）；L458 CALL 行字符串拼接在关开关时仍执行（原有行为，非本次引入）；FileLog 每条日志新增 2×2 次 stat（日志行已节流，可忽略）。无新发现的严重性能隐患。

### 6. 版本一致性 —— PASS（带 1 处注释残留）

- `HdrRatioFix.VERSION` = `Shell.APP_VERSION` = `build.gradle.kts versionName` = **"1.0.0-20260905"** ✓；versionCode 33 ✓
- ksu-module(1.0.1) / ksu-fp16-module(1.1.0) 为独立组件版本，无需同号 ✓
- 残留：L543 javadoc 仍写 "Ratio source (**v2.0.11** — A15 true-division...)"（版本历史注释，无功能影响）；其余 v2.0.x 均为历史沿革注释可留

---

## 二、问题清单

**致命**：无。

**重要**：
1. L271 `apply CALLED`（及 L199 `SYSTEM handle`）仍为 logAlways——关日志开关/disable 后仍持续写 FileLog，违背既定开关语义（一行改动可修）。
2. diag 节流 60s 仅落地 liveDiv 1 处、其余 5 处仍 5000L——与改动声明不一致（高频路径已覆盖，功能无碍）。

**建议**：
3. L543 javadoc "v2.0.11" 注释残留，顺手改掉。
4. L655 日志行 `SysProps.getFloat(PROP_MAX)` 改用 `sCachedUserMax`（同轮已刷新，省一次反射）。
5. PROP_LOG 可纳入 500ms 缓存（log() 每次省一次反射）。
6. legacyWrite L747 `float userMax = sCachedUserMax;` 缩进 8 空格（应 12），纯格式。
7. `f.renameTo(old)` 返回值未检查（理论退化路径，Android 上风险极低）。

---

## 三、结论

**可发版。** 三项核心改动（轮转、开关收窄、缓存）功能正确、并发安全、性能收益明确；编译期验证与逻辑仿真全部通过。两个"重要"项均为日志语义/声明一致性范畴，不涉及功能正确性、稳定性与性能回退；如追求零瑕疵，建议发版前顺手修问题 1（两行 logAlways→log）与问题 3（注释），其余不阻塞。

验证产物：`D:\Lenovo-Tab-UltraHDR-Fix\工具-复核-正式版.py`（9/9 PASS，可重跑）。
